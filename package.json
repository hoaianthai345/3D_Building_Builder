{
  "name": "ai-tour-guide-generator",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm run build --prefix frontend",
    "dev": "npm run dev --prefix frontend",
    "start": "npm run start --prefix frontend",
    "typecheck": "npm run typecheck --prefix frontend"
  }
}

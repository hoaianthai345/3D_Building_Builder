# @google/model-viewer pins an old `three` minor as a peer (it bundles its own
# three internally), which conflicts with the three@0.17x used by react-three-fiber.
# The combination works at runtime; accept the peer mismatch so installs (local +
# Vercel) succeed deterministically.
legacy-peer-deps=true

# Backend URL used by the frontend.
# Local:
NEXT_PUBLIC_API_URL=http://127.0.0.1:8000

# Vercel + local backend via ngrok:
# NEXT_PUBLIC_API_URL=https://your-ngrok-domain.ngrok-free.app

# Static-only frontend mode, disables backend calls:
# NEXT_PUBLIC_API_URL=static
